var express = require('express');
var router = express.Router();
var dataReceiver = require('./model/dataReceiver');

/* GET group dashboard page. */
router.post('/submitData', function(req, res, next) {
  
});

module.exports = router;
